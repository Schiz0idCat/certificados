tcerts automatiza la generación de certificados médicos utilizando plantillas HTML predefinidas.

Este repositorio contiene únicamente el código fuente de la aplicación.
Las plantillas utilizadas en producción son privadas y no se distribuyen públicamente.
Sin estas, el código no compilará.
